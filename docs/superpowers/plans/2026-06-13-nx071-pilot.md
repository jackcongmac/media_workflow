# NX-071 试播集 + 工作流脚手架 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 搭好《今晚值夜班》账号的可复用工作流脚手架，并产出第一条试播集 NX-071（邮递员·一封没送到的信）的完整制作包，交由用户在剪映出片。

**Architecture:** 三层——(1) 文件夹脚手架 + 世界观圣经（可复用模板）；(2) 两个带测试的 shell 工具（建集脚手架 `new_episode.sh`、成片校验 `validate_export.sh`）作为自动化层；(3) NX-071 的全套文本制作包（脚本→分镜→prompt→配音→字幕→质感→发布包）。Agent 产出"到出片为止的一切"，用户在剪映完成 AI 视频生成与剪辑，再用工具校验。

**Tech Stack:** macOS / zsh；ffmpeg + ffprobe；剪映国内版 VIP（剪辑/字幕/AI配音/质感）；即梦/可灵（按需图生视频）；Markdown/CSV/SRT 文本产出。

**Owners:** Claude = 写作室/QA/质量门；Codex = 脚手架/工具/批处理；用户 = 出片/审稿/拍板。
**约束提醒:** 删除任何文件需用户批准（见 spec §7）。

**参考:** 设计稿 `docs/superpowers/specs/2026-06-13-anomaly-archive-universe-design.md`

**约定:** 创意产出类任务用「验收清单」代替单元测试；shell 工具类任务用真实测试（ffmpeg 合成素材）。所有 git 操作前先 `find . -name '._*' -delete 2>/dev/null` 清理 AppleDouble 文件。

---

### Task 0: 预检 — 确认 ffmpeg/ffprobe 可用（Codex）

**Files:** 无（环境检查）

- [ ] **Step 1: 检查工具**

Run: `ffmpeg -version | head -1 && ffprobe -version | head -1`
Expected: 各打印一行版本号。若缺失，提示用户 `brew install ffmpeg` 后再继续。

- [ ] **Step 2: 确认在项目根**

Run: `test -d /Volumes/WorkHD/media_testing/.git && echo OK`
Expected: `OK`

---

### Task 1: 文件夹脚手架 + 每集模板（Codex）

**Files:**
- Create: `shorts/yejiban_anomaly_archive/{00_research,01_scripts,02_prompts,03_images,04_audio,05_video_projects,06_exports,07_publish_packages,08_analytics}/.gitkeep`
- Create: `shorts/yejiban_anomaly_archive/_TEMPLATE/{research.md,script.md,storyboard.csv,image_prompts.md,video_prompts.md,voiceover.txt,captions.srt,texture_notes.md,publish_copy.md}`

- [ ] **Step 1: 建目录树**

```bash
cd /Volumes/WorkHD/media_testing
base=shorts/yejiban_anomaly_archive
for d in 00_research 01_scripts 02_prompts 03_images 04_audio 05_video_projects 06_exports 07_publish_packages 08_analytics; do
  mkdir -p "$base/$d" && touch "$base/$d/.gitkeep"
done
mkdir -p "$base/_TEMPLATE"
```

- [ ] **Step 2: 建每集模板文件（含标题骨架）**

每个模板文件写入对应小标题骨架，例如 `script.md`：
```markdown
# <NX-NNN> 脚本

## 招牌开场句
这是我值夜班第 ___ 天，今晚这段监控，我看了三遍。

## 5 个钩子（备选）
1.
2.
3.
4.
5.

## 规则梗（评论区按钮）
如果你……请不要……

## 戏剧版脚本（60–75s）

## 45 秒精简版

## 配音稿（旁白）

## Beat 对应（见 storyboard.csv）
```
其余文件（research/storyboard/image_prompts/video_prompts/voiceover/captions/texture_notes/publish_copy）各写最小骨架小标题。`storyboard.csv` 首行表头：
`shot,start_s,end_s,desc,camera,subject,texture,first_frame_ref,video_prompt_ref`

- [ ] **Step 3: 验收检查**

Run: `ls shorts/yejiban_anomaly_archive/_TEMPLATE | sort | tr '\n' ' '`
Expected（9 个文件）: `captions.srt image_prompts.md publish_copy.md research.md script.md storyboard.csv texture_notes.md video_prompts.md voiceover.txt`

- [ ] **Step 4: Commit**

```bash
find . -name '._*' -delete 2>/dev/null
git add shorts/
git commit -m "feat(scaffold): yejiban account folder tree + per-episode _TEMPLATE"
```

---

### Task 2: 世界观圣经 SERIES_BIBLE.md（Claude）

**Files:**
- Create: `shorts/yejiban_anomaly_archive/SERIES_BIBLE.md`

- [ ] **Step 1: 写圣经**

内容必须包含（来源 spec §2/§4）：账号设定（不存在事务所 + 夜班监控审核员）；招牌开场句；"如果你…请不要…"梗模板；时长 60–75s；调性（怪谈+情感反转）；质感系统清单；Beat 结构表；世界规则/复现符号（NX 编号、03:17、对讲机三声、7号屏、红色三角贴纸、寻人启事伏笔）；调性红线（不做假新闻/拟真冒充，主动打 AI 标）。

- [ ] **Step 2: 验收清单**

- [ ] 招牌开场句、规则梗模板各出现一次且与 spec 一致
- [ ] 6 个世界符号全部列出（NX编号/03:17/对讲机三声/7号屏/红三角/寻人启事）
- [ ] Beat 结构表 6 行（对应 spec §4.3）
- [ ] 含 AI 标识合规红线

- [ ] **Step 3: Commit**

```bash
find . -name '._*' -delete 2>/dev/null
git add shorts/yejiban_anomaly_archive/SERIES_BIBLE.md
git commit -m "docs(bible): add 今晚值夜班 series bible (world rules + symbols)"
```

---

### Task 3: `tools/new_episode.sh` 建集脚手架工具 + 测试（Codex）

**Files:**
- Create: `tools/new_episode.sh`
- Test: `tools/tests/test_new_episode.sh`

- [ ] **Step 1: 写失败测试**

`tools/tests/test_new_episode.sh`：
```zsh
#!/bin/zsh
set -u
here="${0:A:h}"; root="${here:h:h}"
tmp="$(mktemp -d "$PWD/.tmp_new_episode.XXXXXX")"
trap 'rm -rf "$tmp"' EXIT
mkdir -p "$tmp/tools" "$tmp/shorts/yejiban_anomaly_archive/_TEMPLATE"
for f in research.md script.md storyboard.csv image_prompts.md video_prompts.md voiceover.txt captions.srt texture_notes.md publish_copy.md; do
  : > "$tmp/shorts/yejiban_anomaly_archive/_TEMPLATE/$f"
done
cp "$root/tools/new_episode.sh" "$tmp/tools/new_episode.sh"

MEDIA_WORKFLOW_ROOT="$tmp" "$tmp/tools/new_episode.sh" NX-071 undelivered_letter
[[ $? -eq 0 ]] || { echo "FAIL create exit"; exit 1; }
[[ -d "$tmp/shorts/yejiban_anomaly_archive/NX-071_undelivered_letter" ]] || { echo "FAIL dir"; exit 1; }

MEDIA_WORKFLOW_ROOT="$tmp" "$tmp/tools/new_episode.sh" NX-071 undelivered_letter
[[ $? -eq 4 ]] || { echo "FAIL no-overwrite exit"; exit 1; }

MEDIA_WORKFLOW_ROOT="$tmp" "$tmp/tools/new_episode.sh" BAD-1 x
[[ $? -eq 2 ]] || { echo "FAIL arg-validate exit"; exit 1; }

echo "ALL PASS"
```

- [ ] **Step 2: 跑测试确认失败**

Run: `zsh tools/tests/test_new_episode.sh`
Expected: 失败（`new_episode.sh` 尚不存在）。

- [ ] **Step 3: 写实现**

`tools/new_episode.sh`（采用 Codex 敲定的设计）：
```zsh
#!/bin/zsh
set -u
fail() { code="$1"; shift; print -u2 -- "FAIL: $*"; exit "$code"; }
[[ $# -eq 2 ]] || fail 2 "usage: tools/new_episode.sh <NX-NNN> <slug>"
nx="$1"; slug="$2"
case "$nx" in (NX-[0-9][0-9][0-9]) ;; (*) fail 2 "episode id must match NX-NNN, got: $nx" ;; esac
case "$slug" in (""|*[!A-Za-z0-9_-]*) fail 2 "slug must use only A-Z a-z 0-9 _ -" ;; esac
script_dir="${0:A:h}"
project_root="${MEDIA_WORKFLOW_ROOT:-${script_dir:h}}"
base="$project_root/shorts/yejiban_anomaly_archive"
template="$base/_TEMPLATE"
dest="$base/${nx}_${slug}"
required=(research.md script.md storyboard.csv image_prompts.md video_prompts.md voiceover.txt captions.srt texture_notes.md publish_copy.md)
[[ -d "$template" ]] || fail 3 "template dir missing: $template"
[[ ! -e "$dest" ]] || fail 4 "episode dir already exists: $dest"
for f in "${required[@]}"; do [[ -f "$template/$f" ]] || fail 3 "template missing required file: $template/$f"; done
tmp="$(mktemp -d "$base/.new_episode.${nx}_${slug}.XXXXXX")" || fail 5 "could not create temp dir under: $base"
rsync -a --exclude='._*' --exclude='.DS_Store' "$template"/ "$tmp"/ || fail 5 "template copy failed"
mv "$tmp" "$dest" || fail 5 "could not move temp episode dir into place: $dest"
print -- "$dest"
exit 0
```

- [ ] **Step 4: 跑测试确认通过**

Run: `chmod +x tools/new_episode.sh && zsh tools/tests/test_new_episode.sh`
Expected: `ALL PASS`

- [ ] **Step 5: Commit**

```bash
find . -name '._*' -delete 2>/dev/null
git add tools/new_episode.sh tools/tests/test_new_episode.sh
git commit -m "feat(tools): new_episode.sh scaffolder + tests"
```

---

### Task 4: `tools/validate_export.sh` 成片校验工具 + 测试（Codex）

**Files:**
- Create: `tools/validate_export.sh`
- Test: `tools/tests/test_validate_export.sh`

- [ ] **Step 1: 写失败测试（合成 4 个素材：1 过 / 3 不过）**

`tools/tests/test_validate_export.sh`：
```zsh
#!/bin/zsh
set -u
here="${0:A:h}"; root="${here:h:h}"
fx="$(mktemp -d "$PWD/.tmp_fixtures.XXXXXX")"
trap 'rm -rf "$fx"' EXIT
mk() { # size t audio? out
  local size="$1" t="$2" aud="$3" out="$4"
  if [[ "$aud" == yes ]]; then
    ffmpeg -y -hide_banner -f lavfi -i testsrc2=size=${size}:rate=10 \
      -f lavfi -i sine=frequency=1000:sample_rate=48000 -t "$t" -filter:a "volume=2.2" \
      -c:v libx264 -preset ultrafast -crf 35 -pix_fmt yuv420p -c:a aac -b:a 128k -shortest "$out" >/dev/null 2>&1
  else
    ffmpeg -y -hide_banner -f lavfi -i testsrc2=size=${size}:rate=10 -t "$t" \
      -c:v libx264 -preset ultrafast -crf 35 -pix_fmt yuv420p -an "$out" >/dev/null 2>&1
  fi
}
mk 1080x1920 62 yes "$fx/pass.mp4"
mk 720x1280  62 yes "$fx/fail_res.mp4"
mk 1080x1920 50 yes "$fx/fail_dur.mp4"
mk 1080x1920 62 no  "$fx/fail_noaudio.mp4"

"$root/tools/validate_export.sh" "$fx/pass.mp4";        [[ $? -eq 0 ]]  || { echo "FAIL pass"; exit 1; }
"$root/tools/validate_export.sh" "$fx/fail_res.mp4";    [[ $? -eq 10 ]] || { echo "FAIL res"; exit 1; }
"$root/tools/validate_export.sh" "$fx/fail_dur.mp4";    [[ $? -eq 10 ]] || { echo "FAIL dur"; exit 1; }
"$root/tools/validate_export.sh" "$fx/fail_noaudio.mp4";[[ $? -eq 10 ]] || { echo "FAIL noaudio"; exit 1; }
"$root/tools/validate_export.sh" "$fx/nonexist.mp4";    [[ $? -eq 2 ]]  || { echo "FAIL missing"; exit 1; }
echo "ALL PASS"
```

- [ ] **Step 2: 跑测试确认失败**

Run: `zsh tools/tests/test_validate_export.sh`
Expected: 失败（`validate_export.sh` 尚不存在）。

- [ ] **Step 3: 写实现（采用 Codex 敲定的 ffprobe/ffmpeg 设计）**

`tools/validate_export.sh`：
```zsh
#!/bin/zsh
set -u
fail() { code="$1"; shift; print -u2 -- "FAIL: $*"; exit "$code"; }
[[ $# -eq 1 ]] || fail 2 "usage: tools/validate_export.sh <path-to-mp4>"
mp4="$1"
[[ -f "$mp4" ]] || fail 2 "file not found: $mp4"
command -v ffprobe >/dev/null 2>&1 || fail 2 "ffprobe not installed"

resolution="$(ffprobe -v error -select_streams v:0 -show_entries stream=width,height -of csv=p=0:s=x "$mp4")" || fail 2 "ffprobe video failed"
duration="$(ffprobe -v error -show_entries format=duration -of default=noprint_wrappers=1:nokey=1 "$mp4")" || fail 2 "ffprobe duration failed"
audio_count="$(ffprobe -v error -select_streams a -show_entries stream=index -of csv=p=0 "$mp4" | awk 'NF{n++} END{print n+0}')"

hard_fail=0
res_msg="resolution: $resolution"; [[ "$resolution" == "1080x1920" ]] || { hard_fail=1; res_msg="resolution: $resolution (expected 1080x1920)"; }
awk -v d="$duration" 'BEGIN{exit !(d>=60 && d<=75)}'; dur_ok=$?
dur_msg="duration: ${duration}s OK"; [[ $dur_ok -eq 0 ]] || { hard_fail=1; dur_msg="duration: ${duration}s (expected 60-75)"; }
aud_msg="audio streams: $audio_count OK"; [[ "$audio_count" -ge 1 ]] || { hard_fail=1; aud_msg="audio streams: 0 (expected >=1)"; }

# loudness (warn only)
lufs=""
if [[ "$audio_count" -ge 1 ]]; then
  log="$(ffmpeg -hide_banner -nostdin -i "$mp4" -vn -af loudnorm=I=-14:TP=-1.5:LRA=11:print_format=json -f null - 2>&1)"
  lufs="$(print -- "$log" | awk -F: '/"input_i"/ {v=$2; gsub(/[, "]/,"",v); print v; exit}')"
fi
loud_msg="integrated loudness: ${lufs:-unknown}${lufs:+ LUFS}"
if [[ -n "$lufs" && "$lufs" != "-inf" ]]; then
  awk -v l="$lufs" 'BEGIN{exit !(l < -16 || l > -13)}' && loud_msg="$loud_msg  [WARN outside -16..-13]"
fi

if [[ $hard_fail -eq 0 ]]; then
  print -- "PASS: export validation passed"
  print -- "- $res_msg"; print -- "- $dur_msg"; print -- "- $aud_msg"; print -- "- $loud_msg"
  exit 0
else
  print -u2 -- "FAIL: export validation failed"
  print -u2 -- "- $res_msg"; print -u2 -- "- $dur_msg"; print -u2 -- "- $aud_msg"
  exit 10
fi
```

- [ ] **Step 4: 跑测试确认通过**

Run: `chmod +x tools/validate_export.sh && zsh tools/tests/test_validate_export.sh`
Expected: `ALL PASS`

- [ ] **Step 5: Commit**

```bash
find . -name '._*' -delete 2>/dev/null
git add tools/validate_export.sh tools/tests/test_validate_export.sh
git commit -m "feat(tools): validate_export.sh (1080x1920/60-75s/audio/loudness) + tests"
```

---

### Task 5: 用脚手架建 NX-071 目录（Codex）

**Files:**
- Create: `shorts/yejiban_anomaly_archive/NX-071_undelivered_letter/` (9 文件，来自模板)

- [ ] **Step 1: 建集**

Run: `cd /Volumes/WorkHD/media_testing && MEDIA_WORKFLOW_ROOT="$PWD" ./tools/new_episode.sh NX-071 undelivered_letter`
Expected: 打印 `…/NX-071_undelivered_letter`，退出码 0。

- [ ] **Step 2: 验收**

Run: `ls shorts/yejiban_anomaly_archive/NX-071_undelivered_letter | wc -l | tr -d ' '`
Expected: `9`

- [ ] **Step 3: Commit**

```bash
find . -name '._*' -delete 2>/dev/null
git add shorts/yejiban_anomaly_archive/NX-071_undelivered_letter/
git commit -m "chore: scaffold NX-071 episode dir from template"
```

---

### Task 6: NX-071 历史/逻辑 QA — research.md（Claude）

**Files:**
- Modify: `shorts/yejiban_anomaly_archive/NX-071_undelivered_letter/research.md`

- [ ] **Step 1: 写研究/核实**

必须核实并记录：2006 年（事发设定年）中国邮政平信投递的真实细节（绿色邮筒/邮政自行车/邮戳/平信无寄件人也能投递的逻辑）；"20 年没送到的信"在故事内自洽的设定（事故→执念）；规避真实地名/真人；AI 标识与不做假新闻的红线；1 个中心冲突、1 个情感钩、1 个开放问题、2–4 条可引用来源。

- [ ] **Step 2: 验收清单**

- [ ] 列出 ≥3 条可核查的年代事实（2006 邮政相关），无年代穿帮（对照已修的外卖教训）
- [ ] 故事内设定自洽（为何信 20 年才送 / 为何无寄件人）
- [ ] 明确不用真实地名/真人；含 AI 标识红线
- [ ] 中心冲突/情感钩/开放问题各 1 条

- [ ] **Step 3: Commit**

```bash
find . -name '._*' -delete 2>/dev/null
git add shorts/yejiban_anomaly_archive/NX-071_undelivered_letter/research.md
git commit -m "docs(NX-071): research + historical/logic QA (2006 postal)"
```

---

### Task 7: NX-071 脚本 — script.md（Claude，写作室）

**Files:**
- Modify: `shorts/yejiban_anomaly_archive/NX-071_undelivered_letter/script.md`

- [ ] **Step 1: 写脚本**

按 spec §4.3 Beat 结构写：招牌开场句（值夜班第1天·起源集，植入三条规矩：7号屏别盯太久/03:17卡一下别管/对讲机响三声就归档）；5 个钩子备选；戏剧版（60–75s）；45 秒精简版；规则梗 `如果你深夜收到一封没有寄件人的信，请不要拆开，先看邮戳，是哪一年。`；情感反转（邮递员补送 2006 没送到的信给已长大的孩子）；结尾钩（NX-072 + 7号屏右下角寻人启事）；配音稿（冷静夜班旁白）。

- [ ] **Step 2: 验收清单**

- [ ] 前 3 秒有钩子；招牌开场句在首句
- [ ] Beat 0–4 / 4–15 / 15–35 / 35–60 / 60–70 / 70–75 全覆盖
- [ ] 规则梗一字不差出现；情感反转明确
- [ ] 含 5 钩子 + 戏剧版 + 45s 版 + 配音稿
- [ ] 三个世界符号（03:17 / 对讲机三声 / 7号屏）自然植入
- [ ] 戏剧版口播字数估算落在 60–75s（按 4–5 字/秒，约 260–340 字）

- [ ] **Step 3: Commit**

```bash
find . -name '._*' -delete 2>/dev/null
git add shorts/yejiban_anomaly_archive/NX-071_undelivered_letter/script.md
git commit -m "feat(NX-071): full script (hooks/dramatic/45s/rule line/reversal)"
```

---

### Task 8: NX-071 分镜 — storyboard.csv（Claude + Codex）

**Files:**
- Modify: `shorts/yejiban_anomaly_archive/NX-071_undelivered_letter/storyboard.csv`

- [ ] **Step 1: 写 10–14 镜分镜**

表头：`shot,start_s,end_s,desc,camera,subject,texture,first_frame_ref,video_prompt_ref`。
每镜 4–7s（AI 图生视频单段约 5s，故镜头数需够多才能填满 60–75s），覆盖：监控室宫格/7号屏特写/邮递员夜送/信件邮戳特写/事故重演/对讲机三声/寻人启事反转/归档。`texture` 列标注监控/夜视/时间码处理；`first_frame_ref`/`video_prompt_ref` 关联后两个 prompt 文件的编号。

> 修正记录：原写"6–8 镜/3–6s"在算术上填不满 60–75s（8×6=48s），执行时改为 10–14 镜/4–7s。

- [ ] **Step 2: 验收清单**

- [ ] 10–14 行（不含表头）；每镜 end_s−start_s 在 4–7
- [ ] 各镜 start/end 连续无缝且累加落在 60–75s
- [ ] 每镜有 texture 标注；ref 列编号与 image/video prompts 对得上
- [ ] `column -s, -t storyboard.csv` 可正常对齐（无错位逗号）

- [ ] **Step 3: Commit**

```bash
find . -name '._*' -delete 2>/dev/null
git add shorts/yejiban_anomaly_archive/NX-071_undelivered_letter/storyboard.csv
git commit -m "feat(NX-071): 6-8 shot storyboard"
```

---

### Task 9: NX-071 出图/出视频 prompts（Claude）

**Files:**
- Modify: `shorts/yejiban_anomaly_archive/NX-071_undelivered_letter/image_prompts.md`
- Modify: `shorts/yejiban_anomaly_archive/NX-071_undelivered_letter/video_prompts.md`

- [ ] **Step 1: 写首帧图 prompts（即梦/GPT Image）**

每个分镜一条首帧参考图 prompt，编号对应 storyboard 的 `first_frame_ref`。统一风格关键词：`竖屏 9:16、夜间监控/低照度、写实、无现代穿帮、无画面文字、人物可远可小`。明确"无真人脸特写需求/可侧背光"以降 AI 翻车。

- [ ] **Step 2: 写图生视频 prompts**

每镜一条图生视频 prompt（编号对应 `video_prompt_ref`），描述运动：镜头微动/人物动作/雨雾/灯闪等，3–6s，强调监控质感与轻微失真。注明优先用剪映/即梦自带，质感不足再补可灵。

- [ ] **Step 3: 验收清单**

- [ ] image_prompts 条数 = storyboard 行数；编号一一对应
- [ ] video_prompts 条数 = storyboard 行数；编号一一对应
- [ ] 每条含 9:16 + 监控/夜视质感 + 无画面文字 约束
- [ ] 无真人名/真实地名

- [ ] **Step 4: Commit**

```bash
find . -name '._*' -delete 2>/dev/null
git add shorts/yejiban_anomaly_archive/NX-071_undelivered_letter/image_prompts.md shorts/yejiban_anomaly_archive/NX-071_undelivered_letter/video_prompts.md
git commit -m "feat(NX-071): first-frame image prompts + image-to-video prompts"
```

---

### Task 10: NX-071 配音稿 + 字幕（Claude）

**Files:**
- Modify: `shorts/yejiban_anomaly_archive/NX-071_undelivered_letter/voiceover.txt`
- Modify: `shorts/yejiban_anomaly_archive/NX-071_undelivered_letter/captions.srt`

- [ ] **Step 1: 写配音稿**

`voiceover.txt`：纯旁白文本（无舞台提示），供剪映 AI 配音直接读；与 script.md 戏剧版一致；冷静夜班语气。

- [ ] **Step 2: 写字幕 SRT**

`captions.srt`：标准 SRT 时间轴，分句与 Beat/分镜对齐；每行 7–12 字；每镜只高亮一句关键。

- [ ] **Step 3: 验收清单**

- [ ] `voiceover.txt` 无舞台提示、与脚本一致
- [ ] SRT 时间轴递增、格式合法（`HH:MM:SS,mmm --> HH:MM:SS,mmm`）
- [ ] 末条结束时间落在 60–75s
- [ ] 每行 ≤12 字

- [ ] **Step 4: Commit**

```bash
find . -name '._*' -delete 2>/dev/null
git add shorts/yejiban_anomaly_archive/NX-071_undelivered_letter/voiceover.txt shorts/yejiban_anomaly_archive/NX-071_undelivered_letter/captions.srt
git commit -m "feat(NX-071): voiceover script + caption SRT"
```

---

### Task 11: NX-071 剪映质感处理说明 — texture_notes.md（Claude + Codex）

**Files:**
- Modify: `shorts/yejiban_anomaly_archive/NX-071_undelivered_letter/texture_notes.md`

- [ ] **Step 1: 写剪映操作手册**

针对国内版剪映 VIP 的**逐步操作**：如何叠监控宫格/时间码（含 03:17 卡顿）/夜视绿/噪点/扫描线/对讲机噪声音效；导出设置 1080×1920、目标响度 -14 LUFS、码率建议；AI 标识开关位置与勾选。

- [ ] **Step 2: 验收清单**

- [ ] 覆盖：监控宫格、时间码(含03:17)、夜视/噪点、对讲机三声音效
- [ ] 明确导出 1080×1920 + 响度目标 + 勾选 AI 标识
- [ ] 步骤可被非技术用户照做

- [ ] **Step 3: Commit**

```bash
find . -name '._*' -delete 2>/dev/null
git add shorts/yejiban_anomaly_archive/NX-071_undelivered_letter/texture_notes.md
git commit -m "docs(NX-071): 剪映 texture/export/AI-label how-to"
```

---

### Task 12: NX-071 发布包 — publish_copy.md（Claude）

**Files:**
- Modify: `shorts/yejiban_anomaly_archive/NX-071_undelivered_letter/publish_copy.md`

- [ ] **Step 1: 写发布包**

含：标题（≤12 字、带钩）；3 个标题备选；描述；话题/hashtags（分抖音/小红书/B站）；封面说明；平台分发变体；**AI 生成声明**（合规）；评论区置顶引导（"如果你……请不要……"接梗）。

- [ ] **Step 2: 验收清单**

- [ ] 抖音/小红书/B站 三套话题各≥3 个
- [ ] 含 AI 生成声明
- [ ] 含评论区接梗引导
- [ ] 标题 ≤12 字且含冲突/悬念

- [ ] **Step 3: Commit**

```bash
find . -name '._*' -delete 2>/dev/null
git add shorts/yejiban_anomaly_archive/NX-071_undelivered_letter/publish_copy.md
git commit -m "feat(NX-071): publish package (titles/hashtags/AI disclosure)"
```

---

### Task 13: 出片交接（用户）+ 成片校验

**Files:**
- Create（用户）: `shorts/yejiban_anomaly_archive/NX-071_undelivered_letter/final.mp4`

- [ ] **Step 1: 用户在剪映出片**

用户按 image_prompts/video_prompts 出图与图生视频短镜头（剪映/即梦自带优先，质感不足补可灵），按 storyboard 拼接，套 texture_notes 的质感与字幕、AI 配音，导出 `final.mp4`。

- [ ] **Step 2: 自动校验**

Run: `cd /Volumes/WorkHD/media_testing && ./tools/validate_export.sh shorts/yejiban_anomaly_archive/NX-071_undelivered_letter/final.mp4`
Expected: `PASS: export validation passed`（退出码 0）。不过则按提示在剪映修正重导。

- [ ] **Step 3: 质量门复查（Claude，对照 spec §5）**

逐项核对：竖屏 1080×1920；字幕不裁切；无穿帮现代物/错字；AI 动作无明显穿模；史实不当确定陈述已标注；前 3 秒有钩子；结尾 5 秒引评论；已打 AI 标。

---

### Task 14: 试播验证记录（Claude）

**Files:**
- Create: `shorts/yejiban_anomaly_archive/08_analytics/NX-071_pilot.md`

- [ ] **Step 1: 建测量表**

按 spec §9 建空表：前 3 秒留存、完播率、评论率、是否有人仿写"如果你…请不要…"、是否有人问 NX 编号/03:17/寻人启事（考据信号）；24h/72h/7d 三档。

- [ ] **Step 2: Commit**

```bash
find . -name '._*' -delete 2>/dev/null
git add shorts/yejiban_anomaly_archive/08_analytics/NX-071_pilot.md
git commit -m "docs(NX-071): pilot analytics tracking sheet"
```

---

## Self-Review

**Spec 覆盖核对：**
- §1 战略定位 → 贯穿（脚本/质感/合规约束写入 Task 6/7/11/12）✅
- §2 世界观 → Task 2 SERIES_BIBLE ✅
- §3 A+B 引擎 → A 全覆盖；B（情感微电影）spec 已标"后续单独立 spec"，本计划范围聚焦 A，符合范围切分 ✅
- §4 旗舰格式规范 → Task 2/7/8（招牌句/梗/Beat/符号）✅
- §5 制作工作流 → Task 6–13（含质量门 Task 13 Step 3）✅
- §6 文件夹结构 → Task 1/5 ✅
- §7 分工 → 每任务标 Owner；删除需批准已注明 ✅
- §8 首条样片 NX-071 → Task 5–13 ✅
- §9 验证计划 → Task 14 ✅
- §10 工具链已确认 → Task 9/11/13 体现剪映优先 ✅

**占位符扫描：** 无 TBD/TODO；shell 工具含完整代码；创意任务用明确验收清单替代"写测试"。`script.md` 模板里的 `___` 是招牌句填空（设计要素），非占位符。✅

**一致性核对：** `new_episode.sh` 退出码（0/2/3/4/5）与其测试断言（0/4/2）一致；`validate_export.sh` 退出码（0/2/10）与其测试断言（0/10/10/10/2）一致；模板 9 文件清单在 Task 1/3/5 三处一致；`MEDIA_WORKFLOW_ROOT` 环境变量在工具与测试中用法一致。✅
